import { ReactElement } from 'react';
import './HomePage.css';

const HomePage = (): ReactElement => {
    return (
        <div className='home-view'>
            <h1>Welcome to Manufacturing Engine</h1>
        </div>
    );
};

export default HomePage;